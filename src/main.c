#include <stdio.h>
#include <stdlib.h>
#include "common/defines.h"
#include "utils/format.h"

int main(void) {
  println(D_MSG);
  //char matrix[3][3] = {{'-', '-', 'X'}, {'-', 'X', '-'}, {'X', '-', '-'}};
  //printf("%d\n", verifySecundaryDiagonal(3, matrix, 'X'));
  return 0;
}
